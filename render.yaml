services:
  - type: web
    name: aicrime-auth
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    # Optional: Add PostgreSQL later
    # database:
    #   name: aicrime-db
    #   databaseName: aicrime
    #   user: aicrime_user
